fastapi==0.111.0
uvicorn[standard]==0.29.0
sqlalchemy==2.0.30
psycopg2-binary==2.9.9
python-dotenv==1.0.1
pydantic[email]==2.7.1
anthropic==0.25.0
openai==1.59.9
passlib[bcrypt]==1.7.4
# bcrypt pinned: passlib 1.7.4's startup probe sends a >72-byte password which
# bcrypt >=4.1 rejects with ValueError instead of truncating. 4.0.1 is the last
# version compatible with passlib 1.7.4. Don't bump without testing /auth/signup.
bcrypt==4.0.1
pyjwt==2.10.1
